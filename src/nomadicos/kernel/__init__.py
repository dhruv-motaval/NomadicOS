"""Kernel: the shared, dependency-free foundation every brick may import.

SPEC §53 Phase 1: IDs, errors, events, configuration. No other brick is
imported from here; everything else may import from the kernel.
"""
